function getRandomString(x)
{
	//var t=x,n=Date.now(),r=Math.floor(Math.random()*1e5);
	//n = n.toString(36).toUpperCase();
	//r = r.toString(36).toUpperCase();
	//return (t+n+t+r);
	var n = Date.now();
	return x;
	
}

var t="z",n=Date.now(),r=Math.floor(Math.random()*1e5);
	return n=n.toString(36).toUpperCase(),r=r.toString(36).toUpperCase(),its.cookies.get("xp_ci")+t+n+t+r}
	
	
		char strData[128] = {0};
    errno_t err =  _i64toa_s(1468485222609,strData,sizeof(strData),36);
	char eventtime[MAX_PATH] = {0};
	SYSTEMTIME sysTime;
	time_t unixTime;
	GetLocalTime(&sysTime);
	time(&unixTime);
	sprintf(eventtime,"%ld%d%ld",unixTime,sysTime.wMinute,sysTime.wMilliseconds);